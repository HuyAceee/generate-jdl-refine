export enum StatusEnum {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  REJECTED = 'rejected',
}

export enum RoleEnum {
  USER = 'USER',
  ADMIN = 'ADMIN',
  MANAGER = 'MANAGER',
}

export enum GenderEnum {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}
